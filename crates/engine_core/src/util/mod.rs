//! 内部ユーティリティモジュール。
//!
//! クレート内で共有されるヘルパー関数・型。

#[allow(dead_code)]
pub struct UtilHelper;

impl UtilHelper {
    #[allow(dead_code)]
    #[must_use]
    pub const fn new() -> Self {
        Self
    }
}
